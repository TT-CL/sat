server {
    listen   4201;
    server_name  _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /computation/ {
      proxy_pass http://computation:80;
      rewrite ^/computation(.*)$ $1 break;
    }
}
